name: Github Release

on: workflow_dispatch
  
jobs:
  deploy:
    uses: koliva8245/github.workflows/.github/workflows/github-release.yml@main
    permissions:
      contents: write
    with:
      title: 'Heroes MpqTool'
      app-name: Heroes.MpqTool
      artificats-workflow: build.yml
    secrets:
      artifacts-dowload-token: ${{ secrets.GITHUB_TOKEN }}
      github-release-token: ${{ secrets.GITHUB_TOKEN }}
